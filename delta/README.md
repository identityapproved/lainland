Delta theme config for this repo.

`installers/dev/gitsetup.sh` links this directory to `~/.config/delta` and adds:

`[include] path = ~/.config/delta/themes.gitconfig`

Active delta theme feature:

`[delta] features = lain`
