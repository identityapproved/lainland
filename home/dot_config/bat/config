# bat config — Lain. Palette: lain-colors.md (private repo).
# Theme files live in ./themes; rebuild the cache after changes: bat cache --build
--theme="Lain"
--style="numbers,changes,header"
--italic-text=always
